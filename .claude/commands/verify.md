Run the full verification loop before committing or creating a PR. Fix any issues found.

1. Run `npm run lint` and fix all lint errors
2. Run `npm run test` and fix any failing tests
3. Run `npm run build` and fix any type or build errors
4. Report a summary of what passed and what was fixed
